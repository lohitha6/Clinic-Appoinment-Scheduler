apiVersion: v1
kind: Service
metadata:
  name: {{ include "clinic-scheduler.fullname" . }}
  labels:
    app: {{ include "clinic-scheduler.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
    protocol: TCP
  selector:
    app: {{ include "clinic-scheduler.name" . }}